---
import { YouTube } from "@astro-community/astro-embed-youtube";
import { shorts } from "@/lib/shorts";
import { collections } from "@/lib/collentions";
---

<section
  id="knc-shorts"
  class="flex w-full max-w-5xl flex-col items-center justify-center gap-6 px-4 pt-24"
>
  <h2 class="text-center text-4xl md:text-6xl">KNC Shorts</h2>

  <p class="mb-8 mt-2 max-w-3xl text-center text-base md:text-xl">
    Y cuando nos da la vida y la gana, hacemos cortos, y como somos muy
    creativos los llamamos KNC Shorts. No es KN coches, pero por algo se
    empieza.
  </p>

  <details
    open
    name="studios"
    class="w-full border-b border-knblue py-4 open:border-none"
  >
    <summary class="cursor-pointer p-2 text-xl">Cortos</summary>
    <ul class="grid w-full grid-cols-1 gap-4 md:grid-cols-3">
      {
        shorts.map((id) => (
          <li>
            <YouTube id={id} class="aspect-video w-full rounded-xl" />
          </li>
        ))
      }
    </ul>
  </details>

  <details
    name="studios"
    class="w-full border-b border-knblue py-4 open:border-none"
  >
    <summary class="cursor-pointer p-2 text-xl">Recopilaciones</summary>
    <ul class="grid w-full grid-cols-1 gap-4 md:grid-cols-3">
      {
        collections.map((id) => (
          <li>
            <YouTube id={id} class="aspect-video w-full rounded-xl" />
          </li>
        ))
      }
    </ul>
  </details>
</section>
