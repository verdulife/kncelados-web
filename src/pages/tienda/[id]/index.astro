---
import { getProduct } from "@/lib/services/store";
import Layout from "@/layouts/Layout.astro";
const { id } = Astro.params;

const product = await getProduct(id);
console.log(product);
---

<Layout
  title="Tienda"
  description="Proximamente en la tienda oficial KNC Store, podrás encontrar camisetas, tote bags, láminas, stickers, fondos de pantalla y más."
>
  <section
    class="flex min-h-screen flex-col items-center justify-center gap-8 bg-knc-blue px-4"
  >
    <h1 class="text-4xl font-bold lg:text-8xl">{id}</h1>
  </section>
</Layout>
